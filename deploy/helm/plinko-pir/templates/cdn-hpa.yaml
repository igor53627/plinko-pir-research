{{- if and .Values.cdnMock.enabled .Values.cdnMock.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "plinko-pir.fullname" . }}-cdn-mock
  labels:
    {{- include "plinko-pir.componentLabels" (dict "component" "cdn-mock" "root" .) | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "plinko-pir.fullname" . }}-cdn-mock
  minReplicas: {{ .Values.cdnMock.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.cdnMock.autoscaling.maxReplicas }}
  metrics:
  {{- if .Values.cdnMock.autoscaling.targetCPUUtilizationPercentage }}
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.cdnMock.autoscaling.targetCPUUtilizationPercentage }}
  {{- end }}
{{- end }}
