# Patterns to ignore when building packages
# Keep it simple to avoid helm loading issues

# VCS
.git/
.gitignore

# IDE
.idea/
.vscode/
*.swp

# OS
.DS_Store

# Documentation (keep in chart for reference)
# README.md files are useful in charts

# Scripts are useful for deployment
# scripts/

# Keep all values files available
# values-*.yaml patterns can cause issues
